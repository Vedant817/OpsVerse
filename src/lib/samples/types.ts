export type IncidentSample = {
  id: string;
  label: string;
  title: string;
  module: string;
  scenario: string;
  screenshotNote: string;
  videoNote: string;
  logs: string;
  apiResponse: string;
  dbSnapshot: string;
  gitDiff: string;
};
